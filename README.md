# LAMP Stack Server Setup

This project is a basic LAMP stack setup for running a PHP + MariaDB web application on Apache server.

---

## ⚙️ Stack
- Linux (Rocky / Ubuntu)
- Apache HTTP Server
- MariaDB (MySQL)
- PHP

---

## 🚀 Setup Steps

### 1. Install Apache, PHP, MariaDB
```bash
sudo dnf install httpd php mariadb-server -y
